# Name Server Configuration
PORT=3001

# Database file location (optional, defaults to ./db.json)
# DB_PATH=./data/users.json

# Enable CORS for specific origins (optional, defaults to *)
# CORS_ORIGIN=http://localhost:3000

# Log level (debug, info, warn, error)
LOG_LEVEL=info
